<h1>お気に入り一覧</h1>

<table>
  <tr>
    <th>タイトル</th>
    <th>内容</th>
  </tr>

<% @favorites.each do |favorite| %>
  <tr>
    <td><%= image_tag favorite.picture.image.url %></td>
    <td><%= favorite.picture.content %></td>
  </tr>
<% end %>
</table>

<br>

<%= link_to '投稿一覧画面に戻る', pictures_path %>

<br>

